# server-configs

This is a collection of Buildstruct's server configuration files. This system is for making managing multiple server configurations easier and avoids dealing with Workshop collections.

Majority of config files are used for the Gamestruct server which supports switching between configs.

See `example.toml` for an example config file with annotations.
